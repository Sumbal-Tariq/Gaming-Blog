@model GamingBlog.Models.PCategory

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Dash.cshtml";
}

<h3>Are you sure you want to delete this?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.PCategoryTitle)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.PCategoryTitle)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="col-md-2">
            <span class="btn btn-primary  glyphicon glyphicon-arrow-left" id="myButton">
                @Html.ActionLink("Back", "Index")
            </span>
        </div>
        <div class="form-actions no-color col-md-6">
            <input type="submit" value="Delete" class="btn btn-default" />
        </div>
    }
</div>
