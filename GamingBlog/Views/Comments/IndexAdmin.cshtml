@model IEnumerable<GamingBlog.Models.Comment>

@{
    ViewBag.Title = "IndexAdmin";
    Layout = "~/Views/Shared/_Dash.cshtml";
}

<h2>Comments</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ContentOfComment)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AuthorName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateOfComment)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ContentOfComment)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AuthorName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateOfComment)
        </td>
        <td>
            @*@Html.ActionLink("Edit", "Edit", new { id=item.ID }) |*@
            @Html.ActionLink("Details", "DetailsAdmin", new { id=item.ID }) |
            @Html.ActionLink("Delete", "DeleteAdmin", new { id=item.ID })
        </td>
    </tr>
}

</table>
